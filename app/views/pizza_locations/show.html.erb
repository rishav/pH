<h1><%= @pizza_location.name %></h1>
<% if user_signed_in? && current_user.admin %>
	<%= link_to "Edit", edit_pizza_location_path %>
<% end %>

<p><%= @pizza_location.complete_address %></p>

<% @pizza_location.photos.each do |photo| %>
	<%= image_tag photo.image.url(:thumb )%>
<% end %>